language: node_js
node_js:
- 5.10.1
install:
- cd _preprocess
- npm install
script:
- 
env:
-
after_success:
- if [ $TRAVIS_BRANCH == "master" ]; then
  gulp deploy --host $PRD_FTP_HOST --user $PRD_FTP_USER --password $PRD_FTP_PASSWORD --path $PRD_FTP_PATH;
  else 
  gulp deploy --host $TEST_FTP_HOST --user $TEST_FTP_USER --password $TEST_FTP_PASSWORD --path $TEST_FTP_PATH;
  fi
